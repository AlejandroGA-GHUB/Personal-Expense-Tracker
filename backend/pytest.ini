[pytest]
# Only tests/ is collected. scripts/ holds reset_db.py, which drops every table -
# collecting it would destroy the real database on a plain `pytest` run.
testpaths = tests
python_files = test_*.py
addopts = -q
